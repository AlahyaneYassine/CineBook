<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.cinebook.demo1.ui.controller.AdminShellController"
            styleClass="admin-theme">

    <!-- TOP HEADER -->
    <top>
        <HBox styleClass="cb-header cb-header-admin" alignment="CENTER_LEFT" spacing="16">
            <padding>
                <Insets top="20" right="32" bottom="20" left="32"/>
            </padding>
            
            <!-- Logo/Brand Area -->
            <HBox spacing="12" alignment="CENTER_LEFT">
                <Label text="🎬" style="-fx-font-size: 28px;"/>
                <VBox spacing="2">
                    <Label text="CineBook" styleClass="cb-h2" style="-fx-text-fill: #e2e8f0;"/>
                    <Label text="Administration" styleClass="cb-small" style="-fx-text-fill: #94a3b8;"/>
                </VBox>
            </HBox>
            
            <Region HBox.hgrow="ALWAYS"/>
            
            <!-- User Info & Badge -->
            <HBox spacing="12" alignment="CENTER_RIGHT">
                <VBox alignment="CENTER_RIGHT" spacing="2">
                    <Label fx:id="userNameLabel" text="Admin" styleClass="cb-body" style="-fx-text-fill: #e2e8f0;"/>
                    <Label fx:id="userRoleLabel" text="ADMIN" styleClass="cb-badge-admin"/>
                </VBox>
            </HBox>
        </HBox>
    </top>

    <!-- LEFT SIDEBAR -->
    <left>
        <VBox styleClass="cb-sidebar cb-sidebar-admin" spacing="8" style="-fx-padding: 24px 0;">
            <VBox spacing="12" style="-fx-padding: 0 20px 20px 20px;">
                <Separator/>
            </VBox>

            <VBox spacing="4" style="-fx-padding: 0 12px;">
                <Button text="🎬 Films" fx:id="filmsBtn" onAction="#onNavigateFilms" styleClass="cb-navbtn cb-navbtn-admin" maxWidth="Infinity"/>
                <Button text="🏢 Salles" fx:id="sallesBtn" onAction="#onNavigateSalles" styleClass="cb-navbtn cb-navbtn-admin" maxWidth="Infinity"/>
                <Button text="📅 Séances" fx:id="seancesBtn" onAction="#onNavigateSeances" styleClass="cb-navbtn cb-navbtn-admin" maxWidth="Infinity"/>
                <Button text="🎟 Réservations" fx:id="reservationsBtn" onAction="#onNavigateReservations" styleClass="cb-navbtn cb-navbtn-admin" maxWidth="Infinity"/>
                <Button text="👤 Utilisateurs" fx:id="usersBtn" onAction="#onNavigateUsers" styleClass="cb-navbtn cb-navbtn-admin" maxWidth="Infinity"/>
            </VBox>

            <Region VBox.vgrow="ALWAYS"/>

            <VBox spacing="8" style="-fx-padding: 12px;">
                <Separator/>
                <Button text="🚪 Déconnexion" onAction="#onLogout" styleClass="cb-danger" maxWidth="Infinity"/>
            </VBox>
        </VBox>
    </left>

    <!-- CENTER CONTENT AREA -->
    <center>
        <StackPane fx:id="contentPane" style="-fx-padding: 32px;">
            <!-- Default empty state -->
            <VBox alignment="CENTER" spacing="16" styleClass="cb-empty-state">
                <Label text="🎬" styleClass="cb-empty-state-icon"/>
                <Label text="Sélectionnez une section dans le menu" styleClass="cb-empty-state-text"/>
            </VBox>
        </StackPane>
    </center>

</BorderPane>

